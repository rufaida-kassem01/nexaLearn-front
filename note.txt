1- for warnings of unused var or imports i edit the eslint.config.js file and it has the original file (use it when you finish to check all warnings)
2-i edited the user settings.json file and added the following code to it:  "css.lint.unknownAtRules": "ignore", so it will ignore the warnings of unknown at rules in css files like @theme 
3-all fonts i made in index.css which is in @theme will be removed and replaced with normal one and will edit the code to use them instead of them